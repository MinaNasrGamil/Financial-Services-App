class AppConstants {
  static const String contactEmail = 'support@finserv.com';
  static const String contactPhone = '+201201963754';

  static const Duration apiDelay = Duration(milliseconds: 800);
}
