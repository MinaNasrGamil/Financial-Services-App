// Enum to represent the different states of an operation (e.g., loading, success, error).
enum StatusEnum { initial, loading, success, error }

enum ComplaintTypeEnum { service, billing, product, other }
